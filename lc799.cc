#include<bits/stdc++.h>
class Solution {
public:
    double champagneTower(int poured, int query_row, int query_glass) {
       //I seriously don't know the logic\
       //Pleae forgive. 
    }
};

int main(){
	while(1){
		int poured, q_row, q_column;
		cin>>poured>>q_row>>q_column;
		Solution *S = new Solution();
		double ans = S->champagneTower(poured, q_row, q_glass);
		cout<<ans<<endl;
	}
	return 0;
}
	
